function shareCard() {
  // Génère le QR-code
  alert("QR-code généré ou lien de partage copié !");
}

function contactBy(contactDetails) {
  // Rediriger vers le contac tspécifié
  window.location.href = contactDetails;
}

function downloadVCard() {
  // Logique pour télécharger le vCard
  alert("Téléchargement du vCard lancé !");
}
